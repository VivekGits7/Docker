# Dedicated env for the standalone Redis service. Copy to redis/.env
# Generate a password with: openssl rand -base64 24

REDIS_PASSWORD=replace-with-a-strong-redis-password

# Redis's own network, created automatically when this stack starts.
REDIS_NETWORK=redis-network
